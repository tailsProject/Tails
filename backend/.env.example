# docker-compose.prod.yml 전용 시크릿 템플릿
# 사용법: cp .env.example .env

# RDS 엔드포인트로 교체
SPRING_DATASOURCE_URL=jdbc:mysql://YOUR_RDS_ENDPOINT:3306/tails?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
DB_USERNAME=admin
DB_PASSWORD=changeme

# 프론트 배포 origin (쿠키 기반 refreshToken을 쓰려면 명시적 origin 필요)
CORS_ALLOWED_ORIGINS=https://YOUR_DOMAIN
# 로컬 http 환경은 false, 배포(https) 환경은 true
COOKIE_SECURE=true

# openssl rand -base64 32 등으로 새로 발급
JWT_SECRET=changeme
JWT_EXPIRATION=1800000
JWT_REFRESH_EXPIRATION=1209600000

# 소셜 로그인 성공/이메일 인증/비밀번호 재설정 후 이동할 프론트 페이지
OAUTH2_SUCCESS_REDIRECT_URL=https://YOUR_DOMAIN/oauth2/redirect
APP_EMAIL_VERIFY_URL=https://YOUR_DOMAIN/verify-email
APP_PASSWORD_RESET_URL=https://YOUR_DOMAIN/reset-password

# TourAPI/메일/FCM 선택값
TOUR_API_KEY=
TOUR_API_KEY2=
MAIL_USERNAME=
MAIL_PASSWORD=
FIREBASE_CREDENTIALS_PATH=

# OAuth2는 더미 값 필수
KAKAO_CLIENT_ID=dummy
KAKAO_CLIENT_SECRET=dummy
GOOGLE_CLIENT_ID=dummy
GOOGLE_CLIENT_SECRET=dummy
NAVER_CLIENT_ID=dummy
NAVER_CLIENT_SECRET=dummy
